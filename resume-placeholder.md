# Placeholder resume

This is a placeholder location for a resume file. To add your real resume, upload a PDF named `resume.pdf` to the repository root and replace the resume link in index.html with `/resume.pdf`.
